import styled from "styled-components";

export const HomeWrapper = styled.div`
width: 100%;
padding: 0 80px;
  .footer{
    position: relative;
    width: calc(100% + 160px);
    left: -80px;
  }
  .past-experience {
    margin-top: 20px;
    .section-title{
      font-size: 28px;
      font-weight: 500;
      margin-bottom: 15px;
    }
}
`